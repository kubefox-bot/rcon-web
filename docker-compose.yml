services:
  rcon-dev:
    build:
      context: .
      dockerfile: Dockerfile.full      
    container_name: rcon-dev

    ports:
      - "80:80"                   

    env_file:
      - .env                         
    volumes:
      - .env:/app/.env:ro              