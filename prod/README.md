# 🖥️ CS2 RCON — Продакшн запуск

Инструкция для запуска RCON-интерфейса и backend-сервера через Docker.

---

## 📦 Что находится в этой папке (`prod/`)

```bash
.
├── certs/                 # 📜 TLS-сертификаты (если используешь HTTPS)
├── docker-compose.yml     # 🐳 Конфигурация продакшен-сервиса
├── .env                   # 🔐 Переменные окружения (должен быть создан вручную)
├── .env.example           # 📄 Пример .env файла (можно скопировать)
└── README.md              # 📘 Эта инструкция
