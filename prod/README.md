# 🛠️ Продакшн-инструкция для CS2 RCON

Этот проект предоставляет веб-интерфейс для управления RCON CS2-сервером.

## 📦 Состав

- Бэкенд на Rust (`rcon-api`)
- Фронтенд на Vue 3 (`dist`)
- Nginx — обратный прокси и хостинг фронтенда
- Docker-образ: [`ghcr.io/xymox72/rcon`](https://github.com/users/xymox72/packages/container/package/rcon)

---

## ⚙️ Как использовать

### 1. Создайте `.env` файл на основе `.env.example`

\`\`\`env
AUTH_TOKEN=your_token_here
ENCRYPTION_KEY=12345678901234567890123456789012
PORT=3000
HOST=0.0.0.0
RCON_DOMAIN=yourdomain.com
\`\`\`

> 📌 Если \`RCON_DOMAIN=localhost\` — будет использован **HTTP (порт 80)**.  
> В остальных случаях — **HTTPS (порт 8443)** и нужны сертификаты.

---

### 2. Добавьте сертификаты (если не localhost)

Положите сертификаты в папку `certs/`:

\`\`\`
prod/
├── certs/
│   ├── cert.pem
│   └── key.pem
\`\`\`

---

### 3. Запустите продакшн-контейнер

\`\`\`bash
docker compose -f prod/docker-compose.yml up -d
\`\`\`

---

## 🧪 Проверка

- Фронт доступен по адресу: `http://yourdomain.com` или `https://yourdomain.com:8443`
- API доступно по адресу: `curl http://yourdomain.com/api/health`

---

## 📁 Структура

\`\`\`
prod/
├── certs/              # TLS-сертификаты
├── .env                # Конфигурация приложения
├── docker-compose.yml  # Продакшн-композ
\`\`\`

---

## 💡 Советы

- Никогда не коммитьте `.env` с настоящими токенами
- В будущем можно использовать Docker secrets или volumes для ключей
- Убедитесь, что порт 8443 проброшен на сервере
