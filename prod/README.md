# 💡 RCON UI — Production Setup

Этот проект разворачивает RCON-интерфейс с поддержкой HTTPS через Nginx. 
Подходит для управления CS2-сервером с любого устройства через браузер.

---

## 🚀 Быстрый старт

### 1. Клонируйте проект или скачайте папку `prod`

```bash
git clone https://github.com/your-name/cs2-rcon.git
cd cs2-rcon/prod
```

### 2. Скопируйте и настройте `.env`

```bash
cp .env.example .env
```

Отредактируйте `.env`, задав 32-байтный ключ:

```env
ENCRYPTION_KEY=12345678901234567890123456789012
```

---

### 3. Положите TLS-сертификаты в папку `certs/`

| Файл                            | Описание                                                        |
|--------------------------------|-----------------------------------------------------------------|
| `cert.pem`                     | Публичный TLS-сертификат (Let's Encrypt / Cloudflare / другой) |
| `key.pem`                      | Приватный ключ к сертификату                                   |
| `cloudflare-origin-ca-root.pem`| *(опционально)* корневой сертификат от Cloudflare              |

---

### 4. Задайте домен

Откройте файл `default.conf` и измените строку:

```nginx
server_name rcon.example.com;
```

Например:

```nginx
server_name rcon.batumi.win;
```

---

### 5. Запуск

```bash
docker compose up -d
```

Теперь интерфейс будет доступен по адресу:

```
https://<ваш-домен>:8443
```

---

## 🧰 Состав

- `rcon-back` — Rust-сервер, обрабатывающий команды RCON
- `rcon-front` — Веб-интерфейс для управления
- `nginx` — Reverse Proxy, обеспечивающий HTTPS-доступ

---

## 🛠 Полезные команды

```bash
docker compose up -d        # Запуск в фоне
docker compose logs -f      # Просмотр логов в реальном времени
docker compose down         # Остановка и удаление контейнеров
```

---

## 🔐 Безопасность

- Все пароли шифруются на клиенте с помощью AES-GCM
- Пароли никогда не сохраняются в открытом виде
- Используется `ENCRYPTION_KEY` из `.env` — держите его в секрете

---

## ❓ Поддержка

Если что-то не работает:

```bash
docker compose logs -f nginx
```

Или откройте issue / обратитесь к разработчику ❤️
