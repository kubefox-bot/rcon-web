# 🛠️ Продакшн-инструкция для CS2 RCON

Этот проект предоставляет веб-интерфейс для управления RCON CS2-сервером.

## 📦 Состав

- Бэкенд на Rust (`rcon-api`)
- Фронтенд на Vue 3 (`dist`)
- Traefik — внешний reverse proxy и TLS-терминация
- Docker-образ: [`ghcr.io/xymox72/rcon`](https://github.com/users/xymox72/packages/container/package/rcon)

---

## ⚙️ Как использовать

### 1. Создайте `.env` файл на основе `.env.example`

```env
AUTH_TOKEN=your_token_here
ENCRYPTION_KEY=12345678901234567890123456789012
PORT=3000
HOST=0.0.0.0
RCON_DOMAIN=yourdomain.com
TRAEFIK_ACME_EMAIL=admin@yourdomain.com
```

> 📌 Traefik автоматически выпустит TLS-сертификат Let's Encrypt для `RCON_DOMAIN`.

---

### 2. Проверьте DNS

Домен из `RCON_DOMAIN` должен указывать на сервер, где запускается Compose.

---

### 3. Запустите продакшн-стек

```bash
docker compose -f prod/docker-compose.yml up -d
```

---

## 🧪 Проверка

- Фронт доступен по адресу: `https://yourdomain.com`
- API health: `curl https://yourdomain.com/api/health`

---

## 📁 Структура

```text
prod/
├── .env                # Конфигурация приложения
├── docker-compose.yml  # Продакшн-композ
└── README.md
```

---

## 💡 Советы

- Никогда не коммитьте `.env` с настоящими токенами
- Для выпуска сертификатов порты `80` и `443` должны быть открыты на хосте
- Если уже есть внешний reverse proxy, отключите конфликтующий сервис на 80/443
