services:
  rcon:
    image: ghcr.io/xymox72/rcon-full:latest       # ✅ готовый образ
    container_name: rcon

    ports:
      - "80:80"       # HTTP fallback
      - "8443:8443"   # HTTPS основной

    env_file:
      - .env

    environment:
      - RCON_DOMAIN=${RCON_DOMAIN}  # используется в шаблоне nginx

    volumes:
      - ./certs:/etc/nginx/certs:ro,z   # 🔐 TLS-сертификаты
      - .env:/app/.env:ro,z               # 🔑 для backend (если нужно)
